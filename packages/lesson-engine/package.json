{
  "name": "@aiphone/lesson-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@aiphone/contracts": "workspace:*"
  },
  "devDependencies": {
    "@aiphone/typescript-config": "workspace:*"
  }
}
